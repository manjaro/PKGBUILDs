# arkdep
Toolkit for building, deploying and maintaining a btrfs-based multi-root system

## Patches
### `manjaro.patch`
Changes some hard-coded default values in the script in the upstream project.

The script is meant to be patched like this by upstream so it is self-contained without need for additional config files.
